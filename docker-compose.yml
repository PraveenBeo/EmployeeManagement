services:
  # ---------- Databases ----------
  postgres-usermanagement:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: usermanagement_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata-usermanagement:/var/lib/postgresql/data

  postgres-accesscard:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: accesscard_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - pgdata-accesscard:/var/lib/postgresql/data

  postgres-leave:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: leave_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - pgdata-leave:/var/lib/postgresql/data

  postgres-report:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: report_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5432"
    volumes:
      - pgdata-report:/var/lib/postgresql/data

  # ---------- Message Broker ----------
  rabbitmq:
    image: rabbitmq:3-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "15672:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ---------- APIs ----------
  usermanagement-api:
    build:
      context: .
      dockerfile: src/UserManagement/UserManagement.Api/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres-usermanagement;Port=5432;Database=usermanagement_db;Username=postgres;Password=postgres
      - RabbitMQ__Host=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
    depends_on:
      - postgres-usermanagement
      - rabbitmq

  systemmanagement-api:
    build:
      context: .
      dockerfile: src/SystemManagement/SystemManagement.Api/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__AccessCardConnection=Host=postgres-accesscard;Port=5432;Database=accesscard_db;Username=postgres;Password=postgres
      - ConnectionStrings__LeaveConnection=Host=postgres-leave;Port=5432;Database=leave_db;Username=postgres;Password=postgres
      - ConnectionStrings__ReportConnection=Host=postgres-report;Port=5432;Database=report_db;Username=postgres;Password=postgres
      - RabbitMQ__Host=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
    depends_on:
      - postgres-accesscard
      - postgres-leave
      - postgres-report
      - rabbitmq

  # ---------- API Gateway ----------
  api-gateway:
    build:
      context: .
      dockerfile: src/Gateway/EmployeeManagement.Gateway/Dockerfile
    ports:
      - "5003:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ReverseProxy__Clusters__user-management-cluster__Destinations__destination1__Address=http://usermanagement-api:8080
      - ReverseProxy__Clusters__system-management-cluster__Destinations__destination1__Address=http://systemmanagement-api:8080
      - DownstreamServices__UserManagementApi=http://usermanagement-api:8080
      - DownstreamServices__SystemManagementApi=http://systemmanagement-api:8080
    depends_on:
      - usermanagement-api
      - systemmanagement-api

  # ---------- Web ----------
  web:
    build:
      context: .
      dockerfile: src/Web/EmployeeManagement.Web/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ServiceUrls__GatewayApi=http://api-gateway:8080
    depends_on:
      - api-gateway

volumes:
  pgdata-usermanagement:
  pgdata-accesscard:
  pgdata-leave:
  pgdata-report:
  rabbitmq-data:
